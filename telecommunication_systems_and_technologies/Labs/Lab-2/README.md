# Лабораторная работа №2. *Работа с адресами IP-сетей*

[Файл с полным ТЗ+Теория](Net_LabDescription_2.pdf)

## Цель работы
Получить практические навыки работы с пространством IP-адресов, масками подсетей и управления адресацией в IP-сетях.

## Необходимые знания и программное обеспечение
- Знание двоичной системы счисления и умение переводить числа из десятичной системы в двоичную и обратно
- Среда виртуализации Oracle VirtualBox
- Виртуальная машина Linux (CentOS или Debian)

---

## Краткие теоретические сведения
Все пространство IP-адресов делится на логические группы — IP-сети, предназначенные для организации иерархической адресации в составных IP-сетях. Каждой локальной сети назначается собственная IP-сеть.  
IP-адрес состоит из двух частей: адреса сети и адреса узла. Разделение выполняется с помощью маски подсети, содержащей двоичные единицы в разрядах сетевой части и нули в разрядах узловой части.

Адрес IP-сети — это адрес, в котором все биты узловой части равны нулю.  
Адрес широковещания (broadcast) — адрес, в котором все биты узловой части равны единице.  
Оба адреса являются служебными и не используются для адресации узлов, но входят в диапазон IP-сети.

---

## Часть 1. Расчет и разбиение IP-сетей

1. В работе заданы четыре варианта (таблица вариантов).
2. Для каждого варианта:
    - определить IP-адрес сети;
    - определить адрес широковещания;
    - определить допустимый диапазон IP-адресов для узлов сети.
3. Разделить исходную IP-сеть на подсети в соответствии с заданным количеством компьютеров в каждой локальной сети.
4. Маску подсети выбирать таким образом, чтобы количество доступных IP-адресов было достаточным для соответствующей сети.
5. Выделение подсетей выполнять, начиная с сети с наибольшим количеством узлов.
6. Учитывать, что порт маршрутизатора, подключенный к локальной сети, также имеет IP-адрес.

---

## Часть 2. Проверка расчетов с использованием ipcalc

1. Запустить виртуальную машину Linux (CentOS или Debian) и убедиться в наличии доступа в Интернет.
2. Установить пакет
